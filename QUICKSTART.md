# 快速开始 🚀

## 三种方式使用 un7z

### 1️⃣ Rust 版本（推荐）

**首次使用：**
```bash
# 1. 安装 Rust（如果没有）
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

# 2. 重启终端或
source ~/.cargo/env

# 3. 编译并安装
./build.sh install

# 4. 直接使用
un7z
```

**日常使用：**
```bash
# 交互式选择
un7z

# 批量解压所有
un7z --all

# 带密码
un7z -p "password123"

# 指定目录
un7z -d ~/Downloads
```

### 2️⃣ Bash 版本（备用）

**无需编译，直接运行：**
```bash
./un7z.bash
```

### 3️⃣ 快速编译（不安装）

```bash
# 只编译，不安装
./build.sh

# 运行编译后的二进制
./target/release/un7z
```

## 常用场景

### 场景 1: 下载了大量分卷压缩包
```bash
# 在下载目录运行
cd ~/Downloads
un7z --all
```

### 场景 2: 需要选择性解压
```bash
# 交互式选择
un7z
# 输入: 1,3,5-7
```

### 场景 3: 加密的压缩包
```bash
un7z --password "你的密码"
```

### 场景 4: 验证完整性后解压
```bash
un7z --test --all
```

## 对比原版

| 特性 | Bash 版本 | Rust 版本 |
|------|-----------|-----------|
| 进度更新 | 每个文件完成后 | 实时流畅更新 ✨ |
| 界面 | 简单文本 | 彩色现代化 ✨ |
| 性能 | 较慢 | 快速响应 ✨ |
| 卡死检测 | 不清楚 | 清晰可见 ✨ |
| 日志 | 混乱颜色代码 | 清晰分离 ✨ |

## 文件说明

```
un7z/
├── src/main.rs          # Rust 源代码
├── Cargo.toml           # Rust 依赖配置
├── build.sh             # 编译安装脚本
├── un7z.bash            # 原 Bash 版本（备份）
├── README.md            # 完整文档
├── QUICKSTART.md        # 本文件
└── .gitignore           # Git 忽略规则
```

## 故障排除

### Q: 提示找不到 cargo
A: 需要先安装 Rust:
```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

### Q: 编译失败
A: 确保 Rust 版本 >= 1.70:
```bash
rustc --version
cargo --version
```

### Q: 解压失败
A: 检查是否安装了解压工具:
```bash
# macOS
brew install p7zip unrar

# Linux
sudo apt install p7zip-full unrar
```

## 下一步

- 📖 阅读完整文档: `README.md`
- 🤝 反馈问题: GitHub Issues
- 🎨 自定义配置: 编辑 `src/main.rs`

---

**提示**: Rust 版本编译后是一个独立的二进制文件，可以复制到任何地方使用！
